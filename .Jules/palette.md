## 2025-02-20 - Hidden Key Bindings
**Learning:** Key bindings defined with `show=False` in Textual apps remain invisible to users, significantly hurting discoverability of core features like "Cycle Mode" or "Toggle Tools".
**Action:** Always enable `show=True` for essential bindings and include a `Footer` widget to make them visible and accessible.
